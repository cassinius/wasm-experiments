{
  "scripts": {
    "asbuild:untouched": "asc assembly/floyd.ts -b build/untouched.wasm -t build/untouched.wat --sourceMap --validate --debug",
    "asbuild:optimized": "asc assembly/floyd.ts -b build/optimized.wasm -t build/optimized.wat --sourceMap --validate --optimize",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized"
  },
  "dependencies": {
    "@types/node": "^12.0.10"
  },
  "devDependencies": {    
    "assemblyscript": "github:AssemblyScript/assemblyscript",
    "assemblyscript-loader": "^0.3.0",
    "ts-node-dev": "^1.0.0-pre.40",
    "typescript": "^3.5.2"
  }
}
